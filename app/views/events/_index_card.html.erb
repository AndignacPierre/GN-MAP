<%= link_to event_path(event), class: "card m-2", style: "width: 35rem; text-decoration: none; color: inherit;" do %>
  <div class="d-flex">
    <div class="card-body col-7">
      <h5 class="text-dark-green"><strong><%= event.name %></strong></h5>
      <div class="d-flex align-items-center">
        <img src="<%= asset_path('calendar-icon.png') %>" alt="Calendar Icon" class="me-2 ms-1" style="width: 24px; height: 24px;">
        <p class="card-text mt-0"><%= event.date_event %></p>
      </div>
      <div class="d-flex modal-dialog modal-dialog-centered fw-medium">
        <img src="<%= asset_path('participants-icon.png') %>" alt="Participants Icon" class="me-2 ms-1 mt-1" style="width: 24px; height: 24px;">
        <p class="card-text"><%= event.participants_min %> to <%= event.participants_max %></p>
      </div>
      <div class="d-flex modal-dialog modal-dialog-centered fw-medium">
        <img src="<%= asset_path('location-icon.png') %>" alt="Participants Icon" class="me-2 ms-1" style="width: 24px; height: 24px;">
        <p class="card-text"><%= event.address %></p>
      </div>
      <div class="d-flex justify-content-between modal-dialog modal-dialog-centered fw-medium mt-1">
        <div class="d-flex justify-content-between modal-dialog modal-dialog-centered fw-medium">
          <p class="card-text overflow-hidden"><u><%= event.user.username %></u></p>
        </div>
        <div class="d-flex modal-dialog modal-dialog-centered fw-medium">
          <i><img src="/icons/Star.png" alt="Star Icon" class="icon-class"></i>
          <p class="card-text"><%= 5 %> (<%= rand(1..100) %>)</p>
        </div>
      </div>
    </div>
    <div class="card-body col-5">
      <% if event.photos.attached? %>
        <%= cl_image_tag(event.photos.first.key, class: 'card-img-top p-0', alt: event.name) %>
      <% else %>
        <%= image_tag(asset_path('no_image.png'), class: 'card-img-top p-0', alt: event.name) %>
      <% end %>
    </div>
  </div>
<% end %>
