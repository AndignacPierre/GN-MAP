<img src="<%= event.photos.first %>" class="card-img-top" alt="<%= event.name %>">
<div class="card-body" >
  <h2 class="text-dark-green"><%= event.name %></h2>
  <%= link_to 'Voir cette offre', event_path(event), class: 'btn btn-brown' %>
  <%# <p class="card-text"><%= event.content.truncate(100) %>
</div>
