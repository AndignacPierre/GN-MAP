<div class="navbar navbar-expand-sm navbar-light navbar-lewagon bg-light sticky-top">
  <div class="container-fluid">

    <%= link_to events_path, class: "navbar-brand d-flex logo-container" do %>
      <%= image_tag 'Logo_GN.png' %>
    <% end %>

    <div class="container ms-1" style="width:20%">
      <%= simple_form_for :search, url: events_path, method: :get do |f| %>
        <div class="search-form-control form-group" style="border-radius: 30px; overflow: hidden;">
          <%= f.input :query, label: false, placeholder: "Search in GN MAP ...", wrapper_html: {style: "margin-bottom: 0px !important;"}, input_html: { class: "form-control rounded-pill-input" } %>
          <button type="submit" class="btn btn-flat pt-2">
          <i class="fa-solid fa-magnifying-glass fs-3 icon-blue"></i>
          </button>
        </div>
      <% end %>
    </div>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>

         <li class="nav-item cercle-icon me-1">
          <%= link_to account_subscriptions_path, class: "avatar text-white" do %>
            <i class="fa-solid fa-calendar-days fs-5"></i>
          <% end %>
         </li>



        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link text-white" %>
          </li>
        <% end %>
      </ul>

      <% if user_signed_in? %>
        <div class="d-flex align-items-center">
          <li class="nav-item dropdown d-flex align-items-center">
            <%= render 'layouts/set_vava', vava: current_user %>
            <div class="ms-3 text-white">
              <div><%= current_user.username %></div>
              <div class="user-level" style="font-size: 0.875rem;"><%= "Points: #{current_user.level}" %></div>
            </div>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "My profile", user_path(current_user), class: "dropdown-item" %>
              <%= link_to "Log out", destroy_user_session_path, class: "dropdown-item", data: { turbo_method: :delete } %>
            </div>
          </li>
        </div>
      <% end %>

    </div>
  </div>
</div>
